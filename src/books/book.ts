export interface IBook {
  id: number;
  title: string;
  description: string;
  authors: string;
  favorite: string;
  fileCover: string;
  fileName: string;
}

export interface IParamId {
  id: string;
}
